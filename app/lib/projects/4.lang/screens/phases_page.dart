import 'package:flutter/material.dart';

class PhasesPage extends StatelessWidget {
  const PhasesPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Phases Page")),
    );
  }
}
