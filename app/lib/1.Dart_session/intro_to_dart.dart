/* Section.1 --------------------------------------------------------->
  !~> Intro to programming
  !~> Native vs Cross platform-> (Lang, Performance, size, Resources)
  !~> What is Flutter && Why flutter-> (Lang Biased on, )
  !~> How's work
 */

/* Section.2 --------------------------------------------------------->
  !~> Create first dart App + IDE
  !~> Variables-> (Initialization, Declaration)
  !~> DataTypes-> (String, int, double, boolean, List, Map)
  !~> var vs dynamic && final vs const
  !~> Operators-> (Arithmetic, Relational, typeTest, Bitwise, Assignment, logical)
  !~> Control Statement-> (if, if...else, switch, loop)
  !~> Function-> (return, params [req, optional])
  !~> class-> (constructor, obj, static, inheritance, super, override, abstract)
 */

void main(List<String> args) {
  // print("Hello world");
  // 0~> main + print -------------------------------------------------------------

  // 1~> Variables -------------------------------------------------------------
  // String name = "Aya";

  // 2~> DataTypes -------------------------------------------------------------
  // bool isMale = false; // String & num (int, double) & bool & Collections (list, set, map, Enum, Record, ...) & dynamic & object

  // 3~> final vs const & dynamic vs var ---------------------------------------

  // 4~> Operations (athematic, logical, ....) --------------------------------

  // 5~> Control statement (loop + if) -------------------------------------------------------------

  // 5~> Class -------------------------------------------------------------
}
